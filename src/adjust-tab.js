import { config } from '../app.config'
import TilesetFilter from './tileset-filter'

class AdjustTab {
  constructor(mapObj) {
    this.mapObj = mapObj
    this.tilesetFilter = new TilesetFilters()
  }


  render(node) {
    
  }
}

export default AdjustTab